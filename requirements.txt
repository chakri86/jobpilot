# Web framework
flask>=3.0.0
gunicorn>=21.2.0

# HTTP + scraping helpers
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ── Job scraping (the engine that actually works) ──────────────────────────
# Scrapes Indeed, LinkedIn, ZipRecruiter, Glassdoor with correct selectors.
# Old Copilot code used raw HTML scraping — all 4 sources block that.
python-jobspy>=1.1.0
pandas>=2.0.0

# Resume parsing
pdfplumber>=0.10.0
python-docx>=1.0.0
